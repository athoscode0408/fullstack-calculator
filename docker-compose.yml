services:
  backend:
    build:
      context: ./backend
    environment:
      PORT: "8080"
      ALLOWED_ORIGIN: "http://localhost:3000"
    expose:
      - "8080"

  frontend:
    build:
      context: ./frontend
    depends_on:
      - backend
    ports:
      - "3000:80"
